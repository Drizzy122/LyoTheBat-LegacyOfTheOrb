namespace Platformer
{
    /// <summary>
    /// Item quality tiers, MMO/looter style. Drives tile border color, label color,
    /// and (later) drop rates / vendor pricing.
    /// </summary>
    public enum Rarity
    {
        Common,
        Uncommon,
        Rare,
        Legendary,
        Exotic
    }
}
